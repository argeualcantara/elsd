syntax = "proto3";


package api;

// The els service definitio
service Els {
    // Get a service
    rpc GetServiceInstanceByKey(RoutingKey) returns (ServiceInstance) {}
}

message RoutingKey {
    string id = 1;
}


message ServiceInstance {
    string serviceUri =1;
    string tags= 2;
}
